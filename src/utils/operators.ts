export const increaseByValue = (quantity: number, value: number) => {
  return quantity + value;
};

export const decreaseByValue = (quantity: number, value: number) => {
  return quantity - value;
};
